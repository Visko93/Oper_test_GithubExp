import React from "react";

function SearchComponent(props) {
  return (
    <form onSubmit={() => {}}>
      <header className="form__header">
        <img src={} alt="" className="fomr__header-icon" />
      </header>
    </form>
  );
}

export default SearchComponent;
